import { CircleDollarSign, Landmark, Plus, TrendingUp } from "lucide-react";
import { demoBuildings } from "@/lib/demo-data";
import { compactBrl } from "@/lib/format";

export default function PatrimonioPage() {
  return <div className="content"><div className="page-heading"><div><div className="eyebrow"><Landmark size={13} /> Visão consolidada</div><h1>Patrimônio</h1><p className="subtitle">Todos os ativos pessoais e da holding, em um único contexto.</p></div><button className="button button-primary"><Plus size={15} /> Adicionar ativo</button></div><div className="metrics"><div className="metric-card"><div className="metric-top"><span>Valor total</span><span className="metric-icon"><CircleDollarSign size={15} /></span></div><div className="metric-value">{compactBrl(38_300_000)}</div><div className="metric-foot positive">↑ 4,8% este mês</div></div><div className="metric-card"><div className="metric-top"><span>Ativos</span><span className="metric-icon">07</span></div><div className="metric-value">07</div><div className="metric-foot">3 categorias</div></div><div className="metric-card"><div className="metric-top"><span>Imóveis</span><span className="metric-icon"><Landmark size={15} /></span></div><div className="metric-value">{compactBrl(35_800_000)}</div><div className="metric-foot">93,5% do patrimônio</div></div><div className="metric-card"><div className="metric-top"><span>Variação anual</span><span className="metric-icon"><TrendingUp size={15} /></span></div><div className="metric-value">+12,4%</div><div className="metric-foot positive">R$ 4,2 mi de ganho</div></div></div><div className="panel"><div className="panel-heading"><div><h2>Ativos imobiliários</h2><p>Prédios e propriedades independentes</p></div></div><div className="building-list">{demoBuildings.map((building) => <div className="building-row" key={building.id}><div className="building-thumb" /><div className="building-info"><strong>{building.name}</strong><small>Imóvel · {building.city}, {building.state}</small></div><div className="building-value"><strong>{compactBrl(building.value)}</strong><small className="positive">+ 3,2% no ano</small></div></div>)}</div></div></div>;
}
